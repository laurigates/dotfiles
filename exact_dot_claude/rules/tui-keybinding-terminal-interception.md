# TUI Keybinding "Does Nothing"

Promoted to a skill: invoke `macos-plugin:tui-keybinding-debug` when a
configured key does nothing in a TUI, fzf, or shell app — it diagnoses the
three silent layers (terminal interception under kitty, layout-impossible
chords on non-US layouts, app-side legacy key aliasing).
