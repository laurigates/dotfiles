# Taskwarrior Bulk Operations

Promoted to a skill: invoke `taskwarrior-plugin:task-bulk-ops` when closing,
triaging, or modifying **many** tasks in one pass — it carries the UUID
discipline, `</dev/null`/`xargs` stdin rules, `rc.confirmation=no`, and
filter caveats that make bulk loops safe.
