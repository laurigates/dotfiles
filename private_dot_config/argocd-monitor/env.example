# ArgoCD Monitor Environment Variables
# Copy this file to 'env' and fill in your values
# Do NOT commit the actual 'env' file with secrets!

# Required: ArgoCD server URL
ARGOCD_SERVER=argocd.example.com

# Required: ArgoCD auth token (create with: argocd account generate-token)
ARGOCD_AUTH_TOKEN=

# Required: GitHub token with repo and issues scope
GITHUB_TOKEN=

# Required: GitHub repo for creating issues
ARGOCD_MONITOR_GITHUB_REPO=your-org/your-infrastructure-repo

# Optional: Custom poll interval in seconds (default: 300)
# ARGOCD_MONITOR_INTERVAL=300
