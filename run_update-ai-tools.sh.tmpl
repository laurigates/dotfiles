#!/bin/sh
# Update global AI CLI tools
#
# MCP servers are managed project-by-project via .mcp.json files.
# See /configure:mcp command or MCP Management skill for details.

bun install -g @anthropic-ai/claude-code
bun install -g @google/gemini-cli
