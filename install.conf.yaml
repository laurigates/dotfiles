- defaults:
    link:
        relink: true

- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.tmux.conf: tmux.conf
    ~/.vim_runtime: vim_runtime
    ~/.vimrc: vimrc
    ~/.zshrc: vimrc

- shell:
    - [git submodule update --init --recursive, Installing submodules]
