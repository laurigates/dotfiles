- defaults:
    link:
        relink: true

- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.inputrc: inputrc
    ~/.tmux.conf: tmux.conf
    ~/.vim_runtime: vim_runtime
    ~/.vim_runtime/sources_non_forked/vim-puppet: vim-puppet
    ~/.vim_runtime/my_configs.vim: my_configs.vim
    ~/.vimrc: vimrc
    ~/.oh-my-zsh: oh-my-zsh
    ~/.oh-my-zsh/custom/plugins/zsh-autosuggestions: zsh-autosuggestions 
    ~/.zshrc: zshrc
    ~/.bashrc: bashrc
    ~/.bash_profile: bash_profile
    ~/.profile: profile
    ~/.xprofile: xprofile
    ~/.gitconfig: gitconfig

- shell:
    - [git submodule update --init --recursive, Installing submodules]
